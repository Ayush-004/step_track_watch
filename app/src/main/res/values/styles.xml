<resources>

    <!-- Base application theme for Step Tracker -->
    <style name="Theme.Watch_Step" parent="android:style/Theme.DeviceDefault">

        <!-- Customize your theme here. For example: -->

        <!-- Primary brand color -->
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorPrimaryDark">@color/teal_700</item>

        <!-- Accent color (e.g., for buttons, controls) -->
        <item name="colorAccent">@color/purple_200</item>

        <!-- Background color (or use windowBackground) -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>

        <!-- You can also customize other aspects like window animations,
             divider colors, button styles, etc. -->

    </style>

</resources>